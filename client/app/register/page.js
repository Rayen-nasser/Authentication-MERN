import React from 'react'
import Register from "../components/register"

export const metadata = {'title':'Register'}

const page = () => {
  return (
    <div>
      <Register/>
    </div>
  )
}

export default page
